<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdxt.mapper.ExpireContractMapper">

    <!--查询所有合同-->
    <select id="getAllExpireContract" resultType="com.gdxt.dto.ExpireContractDto">
        select m.name as masterName, m.phone as phone, c.number as contractNumber, c.expire_time as expireTime
        from contract c
        left join master m on c.number = m.contract_number
    </select>


</mapper>
